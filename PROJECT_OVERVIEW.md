# Java-Tetris 專案總覽

這份文件是這組教材的入口頁。
它的目的不是直接講解每一個類別細節，而是幫助學生先建立整體方向：

- 這個專案在做什麼
- 學這個專案可以學到哪些軟體設計觀念
- 三份教學文件各自負責什麼
- 原始碼應該從哪裡開始閱讀

如果把這組教材想成一門課，那這份文件負責的是「地圖」，
[progression.md](C:/Users/sheng/github-repo/java-vibe-william/Java-Tetris/progression.md:1) 負責的是「演化過程」，
[OverviewUML.md](C:/Users/sheng/github-repo/java-vibe-william/Java-Tetris/OverviewUML.md:1) 負責的是「架構視角」。

---

## 一、文件定位

這份文件適合用在以下時機：

- 第一次接觸這個專案時，快速建立整體印象
- 上課前，先說明三份教材的閱讀順序
- 帶學生從「專案目的」走到「實作脈絡」與「類別架構」
- 當學生閱讀原始碼時，作為中途回頭確認方向的索引頁

---

## 二、專案簡介

`Java-Tetris` 是一個以 Java 與 Swing 實作的 Tetris 專案。
它不只是做出一個能玩的遊戲，也很適合作為物件導向與系統演化的教學案例。

從這個專案中，學生可以學到：

- 如何把遊戲拆成資料、畫面、控制三種角色
- 如何從基本類別開始，逐步加上移動、碰撞、消行與計分
- 如何在核心玩法穩定後，再加入音效、特效、模式與高分系統
- 如何使用 `has-a` 與 `is-a` 觀察類別設計

---

## 三、這組教材的三份文件

### 1. `PROJECT_OVERVIEW.md`

角色定位：
這是入口文件，負責建立閱讀順序與整體脈絡。

適合回答的問題：

- 這個專案的教學目標是什麼？
- 我應該先讀哪一份文件？
- 這份專案對應到哪些軟體工程觀念？

### 2. `progression.md`

角色定位：
這是演化式教學文件，負責說明專案如何分三個階段逐步長大。

適合回答的問題：

- 為什麼不一開始就做完整系統？
- 哪些功能應該先做，哪些功能適合後面再加？
- 每一階段新增了哪些類別與責任？

### 3. `OverviewUML.md`

角色定位：
這是架構 UML 教學文件，負責從類別關係與系統分工的角度看整體設計。

適合回答的問題：

- 目前有哪些主要類別？
- 哪些關係是 `has-a`，哪些是 `is-a`？
- 哪個類別負責資料、哪個類別負責畫面、哪個類別負責控制？

---

## 四、建議閱讀順序

建議學生依照下面順序閱讀：

1. 先讀 `PROJECT_OVERVIEW.md`
2. 再讀 `progression.md`
3. 接著讀 `OverviewUML.md`
4. 最後回到 `src/tetris/` 的原始碼逐類別對照

這樣安排的原因是：

- 先知道整體目標，再看功能如何演化
- 先知道演化脈絡，再看類別關係會更容易理解
- 先有 UML 架構印象，再進原始碼比較不容易迷路

---

## 五、三階段學習地圖

### 階段一：基本類別與棋盤骨架

學習焦點：

- `Tetromino` 如何描述方塊
- `Board` 如何保存棋盤資料
- `GamePanel` 如何把資料畫成畫面
- `GameFrame` 與 `Main` 如何建立最基本的執行環境

核心問題：

- 方塊資料與棋盤資料為什麼要拆開？
- 為什麼畫面不應該直接承擔遊戲規則？

### 階段二：移動、碰撞、消橫列與計分

學習焦點：

- `GameState` 如何集中保存遊戲中的主要資料
- `InputController` 如何接收輸入
- `GameController` 如何推進遊戲規則
- `Board` 如何支援碰撞判定、固定方塊與消行

核心問題：

- 鍵盤輸入為什麼不直接修改棋盤？
- 遊戲的更新流程如何從輸入走到重繪？

### 階段三：系統優化與完整體驗

學習焦點：

- `SoundManager` 如何管理音效與背景音樂
- `ScoreManager` 如何保存高分資料
- `Particle`、`Lightning`、`FloatingText`、`BgPiece` 如何支援整體體驗
- `GameState` 與 `GameController` 如何承接模式、選單、暫停與特效

核心問題：

- 哪些功能屬於核心玩法，哪些屬於周邊系統？
- 系統變大之後，如何維持類別責任清楚？

---

## 六、建議原始碼閱讀路線

如果學生準備進入 `src/tetris/` 看實作，建議依照這個順序：

1. `Main.java`
2. `GameState.java`
3. `GameController.java`
4. `InputController.java`
5. `Board.java`
6. `Tetromino.java`
7. `GamePanel.java`
8. `GameFrame.java`
9. `ScoreManager.java`
10. `SoundManager.java`
11. `Particle.java`、`Lightning.java`、`FloatingText.java`、`BgPiece.java`

這樣讀的好處是：

- 先知道程式怎麼啟動
- 再理解遊戲資料與控制流程
- 最後再補畫面細節與周邊系統

---

## 七、類別分工總覽

### 啟動與組裝

- `Main`：建立主要物件，啟動遊戲

### 資料模型

- `GameState`：集中保存遊戲整體狀態
- `Board`：保存棋盤格子資料
- `Tetromino`：保存方塊形狀與旋轉資料

### 控制流程

- `GameController`：執行遊戲規則與狀態轉換
- `InputController`：接收鍵盤輸入並轉交控制器

### 視窗與畫面

- `GameFrame`：Swing 視窗容器
- `GamePanel`：負責畫面繪製

### 服務與體驗

- `SoundManager`：處理 BGM 與音效
- `ScoreManager`：處理高分資料讀寫
- `Particle`、`Lightning`、`FloatingText`、`BgPiece`：處理視覺回饋與背景效果

---

## 八、適合教師帶學生思考的問題

可以請學生在閱讀完三份文件後，回答這些問題：

1. 這個專案中，哪些類別在保存資料？
2. 哪些類別在控制流程？
3. 哪些類別在負責畫面？
4. 哪些類別在擴充體驗，而不是核心規則？
5. 如果要再加入新功能，應該優先放進哪一層？

這些問題的目的，是讓學生不只是「看懂遊戲」，而是真的開始理解系統設計。

---

## 九、教學使用建議

如果把這三份文件安排成一次教學活動，可以這樣進行：

1. 先用這份總覽文件說明課程目標與閱讀順序。
2. 接著用 `progression.md` 說明三個階段如何逐步演化。
3. 再用 `OverviewUML.md` 幫學生把類別關係整理成圖像。
4. 最後回到原始碼，讓學生把文件中的概念對照到實作。

這樣學生會比較容易建立一種觀念：
大型系統不是一口氣完成，而是從基本骨架開始，逐步加入規則、再逐步補上體驗與服務。
